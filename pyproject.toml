[project]
name = "pyfilefinder"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "numpy>=2.4.6",
    "pyside6>=6.11.1",
]

[project.scripts]
findfiles = "pyfilefinder.main:main"